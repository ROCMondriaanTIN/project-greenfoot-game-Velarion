import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

public class BlueHead extends Actor {
    public BlueHead() {
        GreenfootImage image = getImage();
        image.scale(image.getWidth() + 70, image.getHeight() + 70);
        setImage(image);
    }
    public void act() {
        
    }    
}
