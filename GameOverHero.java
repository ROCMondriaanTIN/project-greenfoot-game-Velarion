import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

public class GameOverHero extends Mover {
    public GameOverHero() {
        GreenfootImage image = getImage();
        image.scale(image.getWidth() + 100, image.getHeight() + 100);
        setImage(image);
    }
    public void act() {
       
    }    
}
